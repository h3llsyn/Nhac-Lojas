import 'package:flutter/material.dart';

class AppColors {
  // Cores Principais do Nhac (iguais ao repositório feentzs/Nhac)
  static const Color primary = Color(0xFFFF6961);
  static const Color primaryButton = Color(0xFFFE645C);
  static const Color background = Color(0xFFFFE7E5);
  static const Color textDark = Color(0xFF5D201C);
  static const Color textLight = Color(0xFFFEE3E1);

  // Cores de Suporte e Fundo
  static const Color lightContainer = Color(0xFFFFF2E6);
  static const Color circleBackground = Color(0xFFFFD5D5);
  static const Color selectedChipBg = Color(0xFFFFEBD9);

  // Cores de Input e Bordas
  static const Color hint = Color(0xFFC9BCBC);
  static const Color border = Color(0xFF5D201C);
  static const Color borderLight = Color(0xFFE0E0E0);

  // Cores de Status
  static const Color success = Color(0xFF2EA970);
  static const Color successGreen = Colors.green;
  static const Color warning = Colors.orange;
  static const Color inProgress = Colors.blue;
  static const Color greyText = Colors.grey;
}
